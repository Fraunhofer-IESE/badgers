[tox]
env_list =
    py314
    py313
    py312
    py311
minversion = 4.5.1

[testenv]
description = run the tests with pytest
package = wheel
wheel_build_env = .pkg
deps =
    pytest~=9.0.2
    pytest-timeout~=2.4.0
    pytest-benchmark~=5.2.3
commands =
    pytest -v tests
